From 7f67772aaebddaa2a2890a5008d59fd0061d050b Mon Sep 17 00:00:00 2001
From: Guillaume Fourrier <guillaume.fourrier@epitech.eu>
Date: Fri, 12 Oct 2018 19:25:27 +0200
Subject: [PATCH] Remove build warnings from deprecated fonction and unused
 variables

---
 src/spek-artwork.cc | 2 ++
 src/spek-audio.cc   | 9 ++-------
 2 files changed, 4 insertions(+), 7 deletions(-)

diff --git a/src/spek-artwork.cc b/src/spek-artwork.cc
index 2953808b..cd7e78c7 100644
--- a/src/spek-artwork.cc
+++ b/src/spek-artwork.cc
@@ -13,6 +13,7 @@ class SpekArtProvider : public wxArtProvider
 wxBitmap SpekArtProvider::CreateBitmap(
     const wxArtID& id, const wxArtClient& client, const wxSize& size)
 {
+	(void) id, (void) client, (void) size;
 #ifdef OS_UNIX
     if (id == ART_SPEK) {
         return wxArtProvider::GetBitmap("spek", client, size);
@@ -63,6 +64,7 @@ wxBitmap SpekArtProvider::CreateBitmap(
 
 wxIconBundle SpekArtProvider::CreateIconBundle(const wxArtID& id, const wxArtClient& client)
 {
+	(void) id, (void) client;
 #ifdef OS_UNIX
     if (id == ART_SPEK) {
         return wxArtProvider::GetIconBundle("spek", client);
diff --git a/src/spek-audio.cc b/src/spek-audio.cc
index def6538b..d592add0 100644
--- a/src/spek-audio.cc
+++ b/src/spek-audio.cc
@@ -63,15 +63,10 @@ class AudioFileImpl : public AudioFile
 
 
 Audio::Audio()
-{
-    av_register_all();
-}
+{}
 
 Audio::~Audio()
-{
-    // This prevents a memory leak.
-    av_lockmgr_register(nullptr);
-}
+{}
 
 std::unique_ptr<AudioFile> Audio::open(const std::string& file_name, int stream)
 {
