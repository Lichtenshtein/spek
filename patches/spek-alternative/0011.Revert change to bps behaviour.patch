From 3db4c6bbfa4a73eb706e8e04da95fb0598fe2986 Mon Sep 17 00:00:00 2001
From: withmorten <morten.with@gmail.com>
Date: Wed, 6 Dec 2017 19:19:44 +0100
Subject: [PATCH] Revert change to bps behaviour

---
 src/spek-audio.cc | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/src/spek-audio.cc b/src/spek-audio.cc
index fb6cfb4d..79579902 100644
--- a/src/spek-audio.cc
+++ b/src/spek-audio.cc
@@ -158,7 +158,7 @@ std::unique_ptr<AudioFile> Audio::open(const std::string& file_name, int stream)
             // These decoders set both bps and bitrate.
             bits_per_sample = 0;
         }
-        if (codecpar->codec_id == AV_CODEC_ID_ALAC) {
+        if (bits_per_sample) {
             bit_rate = 0;
         }
         channels = codecpar->channels;
